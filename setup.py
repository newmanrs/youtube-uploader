from setuptools import setup

setup(
    name='youtube_uploader',
    version='0.0.1',
    py_modules=['youtube_uploader'],
    packages=['youtube_uploader'],
    install_requires=[
        'simple-youtube-api',
    ],
)
